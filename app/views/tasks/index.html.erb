<h1>My tasks</h1>
<ul>
  <% @tasks.each do |task|%>
    <li><input type="checkbox"<%= task.completed == true ? "checked" : "unchecked" %>/> <%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<%= link_to 'add new task', tasks_new_path  %>
